[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix backend && npm install --prefix frontend"]

[phases.build]
cmds = ["npm run build --prefix frontend"]

# cache-bust: 2026-04-07-v20

[start]
cmd = "npm start --prefix backend"
